<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#1E1E2C"
    android:fillViewport="true"
    android:padding="16dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <TextView
            style="@style/TitleText"
            android:text="📬 Создать рассылку" />

        <EditText
            android:id="@+id/target_input"
            style="@style/AppEditText"
            android:hint="Получатель (@username)" />

        <EditText
            android:id="@+id/message_input"
            style="@style/AppEditText"
            android:gravity="top"
            android:hint="Текст сообщения"
            android:minLines="3" />

        <EditText
            android:id="@+id/count_input"
            style="@style/AppEditText"
            android:hint="Количество сообщений"
            android:inputType="number" />

        <EditText
            android:id="@+id/delay_input"
            style="@style/AppEditText"
            android:hint="Задержка в секундах (напр., 1.5)"
            android:inputType="numberDecimal" />

        <Button
            android:id="@+id/send_button"
            style="@style/AppButton"
            android:text="🚀 Запустить рассылку" />
            
    </LinearLayout>
</ScrollView>
